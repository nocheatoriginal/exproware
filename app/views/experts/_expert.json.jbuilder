json.extract! expert, :id, :first_name, :last_name, :academic_title, :salary, :travel_willingness, :categories, :profile_image, :biography, :email, :created_at, :updated_at
json.url expert_url(expert, format: :json)
